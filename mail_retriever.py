# this supposedly gets called with a set of input parameters and results in the daily mail (not to be confused with the rightwing UK newspaper)
# getting downloaded to a directory,
# the attached image getting named according to convention, and,
# if successful, a "got new images" flag set True

def retreiver(set_cfg set

):
# take all info required from set_cfg set
# log in
# create file in the appointed directory
# call the function in the set to set the renewSuccess flag = True
